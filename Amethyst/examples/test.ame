// Rolling test file. Feel free to edit when testing forks. Any changes to this file in a PR will likely be accepted.

namespace example;

struct obj {
    overload obj(int x) {
        print(x);
    }

    overload obj(string x) {
        print(x);
    }
}

struct obj2 implements obj {
    overload obj2(int x) : obj(x + 2) {
        
    }

    overload obj2(string x) : obj("hi") {
        print(x);
    }
}

#load
void main() {
    var a = obj2(5);
    var b = obj2("womp");

    amethyst:exit();
}

