// Rolling test file. Feel free to edit when testing forks. Any changes to this file in a PR will likely be accepted.

namespace example;

struct vec {
    int x;
    int y;
    int z;

    vec add(macro vec& b) {
        return { x: this.x + b.x, y: this.y + b.y, z: this.z + b.z };
    }
}

#load
void main() {
    vec a = { x: 1, y: 2, z: 3 };
    vec b = { x: 10, y: 20, z: 30 };

    print(a.add(b));

    amethyst:exit();
}
