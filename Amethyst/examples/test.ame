namespace example;

struct vec {
    int x;
    int y;
    int z;

    // vec(int x, int y, int z) {
    //     this.x = x;
    //     this.y = y;
    //     this.z = z;
    // }

    void test() {
        print(this.x);
        this.x += 10;
    }
}

struct vec2 implements vec {
    void other() {
        print(this);
    }
}

#load
void main() {
    vec2 v = { x: 1, y: 2, z: 3 };
    v.test();
    v.other();
}
