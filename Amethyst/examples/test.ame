// Rolling test file. Feel free to edit when testing forks. Any changes to this file in a PR will likely be accepted.

namespace example;

entitydef entity {
    string id;
}

#load
void main() {
    @/summon wither

    amethyst:dummy e = @e[type="wither", limit=1];
    print(e);

    @/kill @e[type=wither]
    amethyst:exit();
}

