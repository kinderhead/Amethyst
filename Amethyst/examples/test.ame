// Rolling test file. Feel free to edit when testing forks. Any changes to this file in a PR will likely be accepted.

namespace example;

entitydef minecraft:base {
    bool Invulnerable;
    string CustomName;
    unsafe_string[] Tags;
}

entitydef test_entity implements minecraft:base {
    float Health;
}

struct vec {
    int x;
    int y;
    int z;

    overload int sum() {
        return this.x + this.y + this.z;
    }
}

#load
void main() {
    // nbt& data = amethyst:gc/malloc();
    // data = {a: 1};
    // @/tellraw @a "$(data)"

    // print(amethyst:gc/heap);

    vec obj = { x: 1, y: 2, z: 3};
    print(obj.sum());

    amethyst:exit();
}