// Rolling test file. Feel free to edit when testing forks. Any changes to this file in a PR will likely be accepted.

namespace example;

entitydef minecraft:base {
    bool Invulnerable;
    string CustomName;
    unsafe_string[] Tags;
    double[] Pos;
}

entitydef minecraft:sheep implements minecraft:base {
    float Health;
}

int add(macro int arg) {
    return arg + 7;
}

#load
void main() {
    print(add(-8));

    amethyst:exit();
}
