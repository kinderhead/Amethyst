// Rolling test file. Feel free to edit when testing forks. Any changes to this file in a PR will likely be accepted.

namespace example;

entitydef test_entity {
    bool Invulnerable;
}

#load
void main() {
    @/kill @e[type=wither]
    @/summon wither
    
    test_entity e = @e[type="wither", limit=1];
    print(e);
    e.Invulnerable = true;
    @/tellraw @a {nbt:"",entity:"@e[scores={amethyst_id=$(e)},limit=1]"}

    @/kill @e[type=wither]

    //amethyst:exit();
}

