namespace example;

struct vec {
    int x;
    int y;
    int z;

    vec(int x, int y, int z) {
        this.x = x;
        this.y = y;
        this.z = z;
    }

    void test() {
        print(this.x);
        this.x += 10;
    }
}

struct vec2 implements vec {
    vec2(int x, int y, int z) : vec(x * 2, y * 2, z * 2) { }

    void other() {
        print(this);
    }
}

#load
void main() {
    var obj = vec2(1, 2, 3);
    obj.test();
    obj.other();
}
