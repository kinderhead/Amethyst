// Rolling test file. Feel free to edit when testing forks. Any changes to this file in a PR will likely be accepted.

namespace example;

struct obj {
    virtual overload void test(int x) {
        print(x);
    }

    virtual overload void test(string str) {
        print(str);
    }
}

struct obj2 implements obj {
    virtual overload void test(int x) {
        print(x, 2);
    }

    virtual overload void test(string str) {
        print(str, "hi");
    }
}

#load
void main() {
    obj2 x2;
    obj x = x2;
    x.test(5);
    x.test("womp");

    amethyst:exit();
}

