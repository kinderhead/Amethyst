namespace example;

struct vec {
    int x;
    int y;
    int z;

    void test() {
        print(this.x);
        this.x += 10;
    }
}

#load
void main() {
    for (int i = 0; i < 10; ++i) {
        if (i >= 5) {
            print(-i);
        } else {
            print(i);
        }
    }
}

