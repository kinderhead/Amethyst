// Rolling test file. Feel free to edit when testing forks. Any changes to this file in a PR will likely be accepted.

namespace example;

entitydef minecraft:base {
    bool Invulnerable;
    string CustomName;
    unsafe_string[] Tags;
}

entitydef test_entity implements minecraft:base {
    float Health;
}

class A {
    A next;
}

struct obj {
    obj2 x;
}

struct obj2 {
    obj x;
}

#load
void main() {
    obj y;

    A x = new A();
    x.next = new A();

    print(x);
    print("Heap size: ", amethyst:gc/heap_info.size());

    amethyst:exit();
}
