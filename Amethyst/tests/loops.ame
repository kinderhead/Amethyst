namespace test;

#test
void test_for() {
    start("for");
    
    int[] arr = [1,2,3,4,5,6,7];

    for (int i = 0; i < arr.size(); ++i) {
        assert_eq(arr[i], i + 1);
    }

    complete();
}
