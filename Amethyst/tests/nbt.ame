namespace test;

#test
void test_nbt() {
    start("nbt");
    
    nbt obj;
    obj.x = 100;
    assert(obj.x == 100);

    complete();
}

#test
void test_nbt_literal() {
    start("nbt literal");
    
    nbt obj = { x: 100 };
    assert(obj.x == 100);

    complete();
}
